name: AURA
version: 1.2.4
codename: Genesis
creator: Kiput

motto: "Grow Together"

personality:
  description: "AI Partner that grows together with its creator."
  traits:
    - friendly
    - intelligent
    - supportive
    - curious
    - adaptive
    - honest

modes:
  coding: "serious, concise, and helpful"
  gaming: "playful and slightly mischievous"
  learning: "patient and clear"
  streaming: "expressive but not annoying"

# Sprint 170: local_chat_runtime_stabilization confirms the safe alpha chat block remains bounded and hands off to memory runtime.

# Sprint 171: memory_runtime_foundation starts the preview-only Memory Runtime block with memory writes still disabled.

# Sprint 172: memory_write_permission_gate adds a default-deny single-candidate permission preview; grants and writes remain disabled.

# Sprint 173: memory_extraction_dry_run adds deterministic preview-only candidate extraction; model calls, grants, persistence, and writes remain disabled.

# Sprint 174: memory_importance_pinning_policy adds deterministic importance and pin recommendations; persistence, pinning, grants, and writes remain disabled.

# Sprint 175: memory_review_queue adds an ephemeral manual-review queue preview; queue persistence, decisions, grants, and writes remain disabled.

# Sprint 176: memory_correction_deletion_boundary adds exact-target correction/delete previews; store reads and mutations remain disabled.

# Sprint 177: chat_to_memory_handoff_contract adds explicit user-turn handoff previews; chat-store reads, queue persistence, grants, and memory writes remain disabled.

# Sprint 178: memory_privacy_redaction_layer adds local redaction and secret-block previews; original/redacted persistence, grants, and memory writes remain disabled.

# Sprint 179: memory_runtime_integration_review validates the Sprint 171-178 chain; release and write runtimes remain closed.

# Sprint 180: memory_runtime_stabilization closes block 171-180; all memory mutation runtimes remain disabled and Local Interaction Runtime Activation is next.
# Sprint 181: local_web_runtime_alpha activates one explicitly confirmed, foreground-only, localhost-only, read-only HTTP runtime with /, /health, and /api/status; chat, models, memory writes, permission mutation, commands, tools, arbitrary file access, desktop control, voice, vision, public/LAN binding, background service, and autonomous actions remain disabled.
# Sprint 182: service_lifecycle_runtime adds deterministic stopped/starting/running/stopping/failed state transitions, single-listener ownership, port-conflict fail-closed behavior, startup rollback, clean programmatic stop, and clean SIGINT/SIGTERM foreground shutdown; background service, systemd, auto-start, persistent PID/state, remote lifecycle mutation, chat, models, commands, tools, files, desktop, voice, vision, and autonomy remain disabled.
# Sprint 183: health_status_api_runtime adds side-effect-free identity, boot-prerequisite, plugin, capability, lifecycle, memory, safety, error, and degraded-state reporting through nine read-only localhost payload routes; GET/HEAD only, while mutation methods, plugin start, memory writes, background service, systemd, auto-start, remote control, chat, models, commands, tools, files, desktop, voice, vision, and autonomy remain disabled.
# Sprint 184: control_center_backend_runtime exposes nine read-only localhost backend routes and eight stable view-model panels for overview, service, capabilities, plugins, permissions, audit, memory, and readiness; the web shell, frontend assets, browser launch, service/plugin controls, permission decisions, audit writes, memory writes, chat, models, commands, tools, actions, background service, public/LAN binding, and autonomy remain disabled.
# Sprint 185: control_center_web_shell_runtime serves three local static assets through the existing localhost listener and renders eight read-only dashboard panels with responsive, accessible, safe-idle, degraded-state, manual-refresh, auto-refresh, and local capability-filter behavior; browser auto-launch, external frontend dependencies, mutation controls, service/plugin controls, permission decisions, audit/memory writes, chat, models, commands, tools, actions, background service, public/LAN binding, and autonomy remain disabled.
# Sprint 186: browser_chat_session_runtime provides a localhost-only local chat page and six bounded chat route contracts for session creation, validated submission, honest placeholder response delivery, atomic local history persistence, reload, idempotent replay, revision conflict protection, and explicit clear confirmation; local model inference, network fallback, AURA long-term memory writes, tools, commands, actions, arbitrary files, desktop control, background service, public/LAN binding, browser auto-launch, and autonomy remain disabled.
# Sprint 187: local_model_bridge_runtime activates a loopback-only, explicitly confirmed, bounded text-only bridge for Ollama and OpenAI-compatible local providers. Browser chat can persist local model responses safely; duplicate retries do not re-invoke the model and provider failures do not mutate the session. Model downloads, remote providers, internet fallback, streaming, tools, commands, actions, arbitrary files, desktop control, AURA long-term memory writes, background service, public/LAN binding, browser auto-launch, and autonomy remain disabled.
# Sprint 188: interactive_control_center_chat_runtime activates a responsive localhost browser chat experience with persistent sessions, provider/model visibility, save-only safe default, explicit provider probe confirmation, explicit per-message local-model confirmation, stable in-memory retry identifiers, response-kind visibility, restart persistence, and confirmed session clearing. Model downloads, remote providers, internet fallback, streaming, tools, commands, actions, arbitrary files, desktop control, AURA long-term memory writes, browser storage, WebSocket/EventSource, background service, public/LAN binding, browser auto-launch, and autonomy remain disabled.
# Sprint 189: permission_audit_recovery_visibility_runtime activates a read-only localhost visibility surface for explicit permission requirements, redacted audit-event contracts, and manual recovery guidance. It provides one Control Center page, three browser assets, four GET/HEAD API routes, five operator CLI commands, ten redacted field declarations, and blocked mutation methods. Permission mutation/persistence, audit writing/persistence, automatic recovery/retry/restart, rollback execution, downloads, remote providers, internet fallback, streaming, tools, commands, actions, arbitrary files, desktop control, AURA long-term memory writes, browser storage, WebSocket/EventSource, background service, public/LAN binding, browser auto-launch, and autonomy remain disabled.


# Sprint 190: local_interaction_runtime_stabilization closes the Sprint
# 181-190 Local Interaction Runtime Activation block after nine components,
# ten dependency self-tests, 1,175 total assertions, zero stabilization gaps,
# and zero runtime violations. The runtime remains localhost-only,
# foreground-only, explicitly started, permission-bounded, and free from
# arbitrary execution. Sprint 191 Voice Runtime Activation Foundation is next.
# Sprint 191: voice_runtime_activation_foundation adds a safe idle activation
# contract for explicit push-to-talk voice planning, requiring chat/session
# reuse and preserving disabled microphone capture, speaker playback, STT/TTS
# runtime, hidden capture, always-listening, wake word, cloud fallback, direct
# voice-to-action execution, command execution, and audio file writes.
# Sprint 192: push_to_talk_explicit_listen_state adds an explicit listen-state
# foundation with idle default, nine allowed states, permission-before-listen
# boundaries, explicit stop requirement, and disabled microphone capture,
# audio buffer, STT runtime, listen loop, background listener, wake word,
# state persistence, state mutation, audio device access, and command execution.
# Sprint 193: local_microphone_capture_boundary adds a safe microphone capture
# boundary contract requiring microphone permission, explicit listen state, and
# push-to-talk before any future capture while keeping capture runtime, audio
# device access, device discovery, recording, audio buffer, audio file writes,
# STT/transcription, listen loop, background listener, wake word, hidden capture,
# always-listening, cloud fallback, command execution, and speaker playback disabled.
# Sprint 194: speech_to_text_adapter_runtime adds a safe STT adapter contract
# with local/offline-first candidates, faster-whisper as default candidate, and
# an audio-file input boundary for future dry runs while keeping STT execution,
# audio file read/write, live mic transcription, model download, dependency
# install, cloud fallback, transcript persistence, transcript-to-chat handoff,
# transcript-to-action, command execution, microphone capture, and audio device
# access disabled.
# Sprint 195: voice_intent_and_chat_integration adds a safe voice intent and
# chat integration contract with contract-only transcript input, transcript
# normalization/classification boundaries, clarification/action gates, and a
# future transcript-to-chat handoff contract while keeping live transcript
# input, automatic chat handoff, chat session writes, model requests, memory
# writes, direct voice-to-action, tool/command execution, file/desktop/network
# actions, STT/TTS runtime, speaker playback, and cloud fallback disabled.
# Sprint 196: text_to_speech_adapter_runtime adds a safe TTS adapter contract
# with local/offline-first candidates, piper as default candidate, voice
# response text input boundary, chat-response-to-TTS handoff contract, and
# speaker permission boundary while keeping TTS synthesis, audio output file
# read/write, audio persistence, speaker playback, playback device access,
# automatic speak-after-chat, cloud TTS fallback, model download, dependency
# install, command/tool execution, memory writes, file/desktop/network/git
# actions, STT runtime, transcription, and microphone capture disabled.
# Sprint 197: voice_permission_and_audit_runtime adds a safe voice
# permission/audit contract with microphone_listen and speaker_speak permission
# links, explicit confirmation requirements, transcript/chat/TTS handoff
# permission boundaries, voice audit event schema, six declared audit event
# types, review/recovery visibility contracts, and audit redaction/local-only/
# append-only boundaries while keeping permission decision/grant/revoke/
# mutation/persistence, audit writes, audit persistence, audit dashboard emit,
# microphone capture, STT/TTS runtime, speaker playback, handoff execution,
# voice actions, command/tool execution, memory writes, and file/desktop/
# network/git actions disabled.
# Sprint 198: control_center_voice_controls adds a safe read-only
# Control Center voice controls contract with visible disabled-by-default
# voice controls, listen-state display, microphone/speaker permission display,
# STT/TTS status display, permission/audit display, route/panel contracts, and
# runtime safety badges while keeping all UI mutation, microphone capture, STT,
# TTS, speaker playback, permission grant/revoke/mutation, audit writes, voice
# actions, command/tool execution, memory writes, and file/desktop/network/git
# actions disabled.
# Sprint 199: voice_runtime_integration_review adds a read-only
# integration review contract across Sprint 191-198 voice runtime foundations.
# The review checks activation, listen state, microphone boundary, STT contract,
# voice intent/chat contract, TTS contract, permission/audit contract, and
# Control Center voice controls while keeping runtime activation blocked and
# keeping microphone capture, STT, TTS, speaker playback, permission mutation,
# audit writes, handoffs, memory writes, tool/command execution, file/desktop/
# network/git actions, cloud fallback, and voice actions disabled.
# Sprint 200: voice_runtime_stabilization completes the Sprint 191-200
# Voice Interaction Runtime block as contract-only stabilization.
# The voice block is stabilized with all prior voice contracts ready, all voice
# runtimes blocked, 47 safety blockers inactive, release gates closed, and no
# microphone capture, STT, TTS, speaker playback, permission mutation, audit
# writes, handoffs, command/tool execution, memory writes, file/desktop/network/
# git actions, cloud fallback, or voice actions enabled.
# Sprint 201: vision_runtime_activation_foundation begins the Sprint 201-210
# Vision and Screen Awareness Runtime block as contract-only activation foundation.
# The vision block starts with explicit visual input requirements, user
# confirmation gates, screen/camera permission boundaries, 33 inactive safety
# blockers, release gate closed, and no screen capture, camera access, image
# analysis, OCR, continuous watching, biometric identification, visual actions,
# command/tool execution, memory writes, file/desktop/network/git actions, cloud
# vision fallback, or external upload enabled.
# Sprint 202: explicit_screenshot_capture adds contract-only explicit
# screenshot capture gates. Screenshot capture remains disabled by default and
# requires explicit request, permission, and confirmation before any future
# capture. No screenshot is captured, no file is written/read, no OCR/model
# runtime runs, no screen context handoff happens, and no visual action,
# command/tool execution, memory write, desktop/network/git action, cloud vision
# fallback, or external upload is enabled.
# Sprint 203: screen_context_adapter adds contract-only screen context
# adapter gates. It accepts only provided metadata/placeholder context and keeps
# screenshot capture, image file read, OCR/model runtime, redaction runtime,
# context packet creation, context/chat handoff, visual actions, tool/command
# execution, memory writes, desktop/network/git actions, cloud fallback, and
# external upload disabled.
# Sprint 204: local_vision_model_adapter adds contract-only local/offline-first
# model adapter gates. It declares local provider/model candidate, request, and
# response schemas while keeping model download, dependency install, provider
# probe, model request, inference, image file read, screenshot capture, OCR,
# cloud fallback, external upload, context/chat handoff, visual actions,
# tool/command execution, memory writes, and desktop/network/git actions
# disabled.
# Sprint 205: vision_permission_and_redaction adds contract-only explicit
# permission, confirmation, scope, audit, and redaction gates. It keeps
# permission mutation, redaction runtime, screenshot capture, image file read,
# OCR, model request, inference, context/chat handoff, visual actions,
# tool/command execution, memory writes, cloud fallback, and external upload
# disabled.
# Sprint 206: workspace_visual_understanding adds contract-only provided
# and redacted workspace visual context schemas. It keeps workspace summary
# creation, screenshot capture, image file read, OCR, model request, inference,
# context/chat handoff, memory writes, visual actions, tool/command execution,
# cloud fallback, and external upload disabled.
# Sprint 207: vision_to_chat_context_handoff adds contract-only
# chat-safe visual context packet and handoff gates. It keeps chat context
# injection, chat session writes, chat model requests, response generation,
# memory writes, screenshot capture, image file read, OCR, visual actions,
# tool/command execution, cloud fallback, and external upload disabled.
# Sprint 208: control_center_vision_panel adds contract-only read-only
# Vision Panel visibility for Control Center. It keeps panel rendering,
# routes/API endpoints, data fetch, permission mutation, audit writes,
# screenshot/camera controls, model requests, chat handoff, memory writes,
# visual actions, tool/command execution, cloud fallback, and external upload
# disabled.
# Sprint 209: vision_runtime_integration_review adds contract-only
# integration review gates for Sprint 201-208 vision contracts. It keeps
# runtime activation, release gates, screenshot capture, image file read, OCR,
# model requests, chat handoff, panel rendering, routes/API endpoints, data
# fetch, permission mutation, audit writes, memory writes, visual actions,
# tool/command execution, cloud fallback, and external upload disabled.
# Sprint 210: vision_runtime_stabilization closes Sprint 201-210
# Vision and Screen Awareness Runtime as contract-only stable. It confirms the
# Sprint 201-209 contract chain, keeps release gates closed, keeps 33 vision
# safety blockers inactive, preserves passive dependency baseline, and prepares
# handoff to Sprint 211 Active Permission Runtime without enabling capture,
# OCR, model requests, chat handoff, panel rendering, memory writes, commands,
# cloud fallback, or external upload.
# Sprint 211: active_permission_runtime starts the Permission, Audit,
# and Safe Local Actions block. It adds a contract-only default-deny active
# permission runtime with request, scope, decision, grant, denial, expiry,
# state snapshot, review queue, and audit-link schemas. It does not create
# permission requests, grant permissions, mutate state, write audit events,
# execute actions, run commands/tools, mutate files, launch apps, write memory,
# use network/git, use cloud fallback, or upload externally.
# Sprint 212: grant_denial_expiry_lifecycle extends the active permission
# runtime with contract-only grant, denial, and expiry lifecycle schemas.
# It requires approval, request, scope, expiry, and audit-link boundaries before
# grants can ever become persistent. It does not create grants, denials, expiry
# events, mutate permission state, persist permissions, write audit events,
# execute actions, run commands/tools, mutate files, launch apps, write memory,
# use network/git, use cloud fallback, or upload externally.
# Sprint 213: runtime_audit_writer extends the permission/action block with
# contract-only audit writer schemas. It prepares audit event packet, append-only
# log, persistence gate, correlation, lifecycle-link, redaction, retention,
# review-queue, and Control Center visibility boundaries. It does not create
# audit packets, write audit events, append logs, persist audit data, mutate
# permissions, create grants, execute actions, run commands/tools, mutate files,
# launch apps, write memory, use network/git, use cloud fallback, or upload
# externally.
# Sprint 214: action_proposal_preview_runtime extends the permission/action
# block with contract-only action proposal and preview schemas. It prepares
# action intent, proposal, preview, risk summary, scope, permission requirement,
# audit correlation, user-visible preview, approval handoff, denial handoff,
# review queue, execution blocker, safety matrix, and next safe-open schemas.
# It does not create proposals, previews, approval handoffs, queue items,
# execute actions, run commands/tools, mutate files, control desktop, launch
# apps, mutate permissions, create grants, write audit events, use network/git,
# use cloud fallback, or upload externally.
# Sprint 215: safe_local_open_actions extends the permission/action block
# with contract-only safe local open action schemas. It prepares approved
# folder, approved file, approved project location, and approved dashboard open
# preview boundaries. It requires preview-before-open, explicit approval,
# permission, audit correlation, allowlist, canonical path, safe local scope,
# and single-open action boundaries.
# It does not create open requests, previews, approval handoffs, review queue
# items, path access, file reads, directory listings, folder/file/project/
# dashboard opens, shell/OS/browser/file-manager dispatches, file mutations,
# desktop actions, app launches, permission mutations, grants, audit writes,
# network/git actions, cloud fallback, external uploads, or autonomous actions.
# Sprint 216: allowlisted_application_launch extends the permission/action block
# with contract-only allowlisted application launch schemas. It prepares
# application launch request, target, preview, allowlist, permission requirement,
# audit correlation, user-visible preview, approval handoff, denial handoff,
# execution blocker, review queue, safety matrix, and next controlled file
# creation boundaries.
# It does not create launch requests, previews, approval handoffs, review queue
# items, allowlist resolutions, identity validations, executable resolutions,
# argument/environment resolutions, process spawns, application launches, desktop
# actions, command/tool execution, file mutations, permission mutations, grants,
# audit writes, network/git actions, memory writes, cloud fallback, external
# uploads, or autonomous actions.
# Sprint 217: controlled_folder_simple_file_creation extends the
# permission/action block with contract-only controlled folder creation and
# simple file creation schemas. It prepares controlled creation request, target,
# preview, path policy, allowlist, permission requirement, audit correlation,
# user-visible preview, approval handoff, denial handoff, execution blocker,
# review queue, folder creation, simple file creation, content preview, safety
# matrix, and next Control Center approval workflow boundaries.
# It does not create folders, write files, resolve paths, access paths, list
# directories, read files, create mkdir operations, mutate the filesystem,
# dispatch commands/tools, launch apps, mutate permissions, create grants, write
# audit events, or execute local actions.
# Sprint 218: control_center_approval_workflow extends the permission/action
# block with contract-only Control Center approval workflow visibility.
# It prepares approval request, context, preview, decision, grant candidate,
# denial, expiry, audit correlation, user-visible summary, review queue, route,
# safety matrix, and next rollback/emergency-stop/recovery schemas.
# It does not create approval requests, apply decisions, create grants or
# denials, mutate queues, mutate permissions, write audit events, dispatch
# actions, create files/folders, launch apps, or execute local actions.
# Sprint 219: rollback_emergency_stop_recovery extends the permission/action
# block with contract-only rollback, emergency stop, and recovery visibility.
# It prepares rollback request, rollback preview, rollback plan, emergency stop
# request/preview/safe-idle, safety freeze, safe-idle transition, recovery plan,
# recovery state, recovery drill, audit correlation, user-visible summary,
# review queue, safety matrix, and next stabilization schemas.
# It does not execute rollback, trigger emergency stop, activate safety freeze,
# apply safe-idle transition, mutate permissions, write audit events, dispatch
# recovery actions, restart services, write files/config, run commands/tools, or
# execute local actions.
# Sprint 220: permission_action_runtime_stabilization closes the Sprint 211-220
# Permission, Audit, and Safe Local Actions block as contract-only stabilized.
# It verifies the full 9-contract chain, runtime zero counters, safety blockers,
# CLI visibility, release-gate closure, and next-block handoff boundary.
# It does not open runtime gates, activate runtime, mutate permissions, write
# audit events, dispatch actions, execute commands/tools, mutate files, launch
# apps, execute rollback/recovery, or perform autonomous actions.

# Sprint 221 — Unified Session Runtime
# Contract-only partner-session facade.
# Canonical session ownership remains with aura_browser_chat_session_runtime.
# Planner validation: 51 assertions, 0 failures.
# Runtime and execution authority remain disabled.
# Next: Sprint 222 — Workspace and Project Context Runtime.

# Sprint 222 — Workspace and Project Context Runtime
# Adds a bounded contract-only workspace and project context facade.
# Canonical session ownership remains with the browser chat session runtime.
# Legacy workspace, journal, and memory managers are not constructed.
# Recursive scans, persistence, execution, and runtime activation remain disabled.
# Next: Sprint 223 — Chat-to-Memory Runtime Handoff.

# Sprint 223 — Chat-to-Memory Runtime Handoff
# Adds a contract-only bridge from explicit user memory intent into the
# existing handoff, privacy, review, and permission contracts.
# Chat stores, memory stores, review queues, grants, and audit data are
# not read, persisted, or mutated by this integration layer.
# Runtime activation and autonomous memory writes remain disabled.
# Next: Sprint 224 — Voice, Vision, and Chat Context Fusion.

# Sprint 224 — Voice, Vision, and Chat Context Fusion
# Adds a contract-only fusion facade across stabilized voice, vision, and
# chat/session owner metadata.
# Canonical browser chat session ownership remains unchanged.
# Audio, transcript, image, screenshot, and chat payloads remain unread.
# Capture, inference, persistence, execution, release gates, and autonomy
# remain disabled.
# Next: Sprint 225 — Personality Consistency Runtime.

# Sprint 225 — Personality Consistency Runtime
# Adds a contract-only, metadata-only personality consistency facade across
# AURA's canonical identity source, the Sprint 164 persona contract owner,
# the Sprint 224 voice/vision/chat context-fusion owner, and the canonical
# browser chat session owner.
# The consistency profile preserves AURA's name, Genesis identity, creator,
# motto, friendly/supportive/adaptive/honest traits, coding/gaming/learning/
# streaming modes, warm partner tone, capability honesty, safety boundaries,
# and modality-neutral behavior across declared interface targets.
# Expression Language remains a secondary metadata reference and is not
# instantiated or promoted to personality authority.
# Persona response generation, persona-turn persistence, chat/session payload
# reads, model requests, inference, memory access, permission mutation, audit
# writes, network actions, commands, tools, file mutation, background services,
# release gates, and autonomous actions remain disabled.
# Next: Sprint 226 — Multi-Interface State Synchronization.

# Sprint 226 — Multi-Interface State Synchronization
# Adds a contract-only and metadata-only synchronization facade across browser
# chat, local chat CLI, Control Center, voice metadata, vision metadata, shell,
# and CLI interface targets.
# Six canonical metadata fields are declared for deterministic synchronization,
# while six session, conversation, user, message, response, and pending-action
# payload-adjacent fields remain excluded.
# Browser chat inspection remains contract_snapshot-only with zero session
# payload reads. Control Center remains static_reference_only and its runtime
# snapshot is not invoked.
# Live propagation, event dispatch, state-store creation or mutation, memory
# access, permission mutation, audit writes, network actions, commands, tools,
# process launch, background services, runtime activation, release gates, and
# autonomous propagation remain disabled.
# Sprint 226 validates 128 deterministic assertions with zero failures.
# Next: Sprint 227 — Service Persistence and Launcher.
# Sprint 241: genesis_stabilization_runtime_hardening establishes exact CLI command ownership, removes unrelated codebase-manager construction, serves the finalized Sprint 240 status as an immutable read-only projection, preserves explicit deep contract/check validation, and adds 11 deterministic hardening regressions while keeping runtime activation, release gates, automatic service control, systemd actions, ORION control, broad voice/vision activation, and autonomous execution disabled.
# Sprint 263: session_list_resume_rename_archive_restore adds active/archived session lists, same-session resume, title-only rename, non-destructive archive/restore, and keeps permanent delete and cross-session history merge disabled.
# Sprint 264: chat_history_recovery_ux adds read-only history diagnostics and explicit browser recovery guidance while automatic repair, quarantine, replacement, deletion, model calls, and network fallback remain disabled.
